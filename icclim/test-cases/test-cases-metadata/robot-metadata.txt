*** Settings ***
Library    OperatingSystem
Library    Process
Library    String
Resource   ../default_variables.txt

*** Variable ***
${TEST_CATEGORY}    metadata

# Optional arguments:
# --test metadata-01-fd.tst   # To include only this test case
# --variable OUTPUT:output-folder-name
# --variable INPUT:input-test-data-folder

# Comments can be written like this

*** Test Cases ***
metadata-01-fd.tst
    ${handle} =         Run Process      test-cases/icclim-test-wrapper.py       -t    test-cases/test-cases-metadata/${TEST_NAME}    -o     ${OUTPUT}    -i    ${INPUT}     stdout=stdout_${TEST_CATEGORY}_${TEST_NAME}.txt    stderr=stderr_${TEST_CATEGORY}_${TEST_NAME}.txt
    Should Be Equal As Integers          ${handle.rc}    0

metadata-02-su.tst
    ${handle} =         Run Process      test-cases/icclim-test-wrapper.py       -t    test-cases/test-cases-metadata/${TEST_NAME}    -o     ${OUTPUT}    -i    ${INPUT}     stdout=stdout_${TEST_CATEGORY}_${TEST_NAME}.txt    stderr=stderr_${TEST_CATEGORY}_${TEST_NAME}.txt
    Should Be Equal As Integers          ${handle.rc}    0

metadata-03-id.tst
    ${handle} =         Run Process      test-cases/icclim-test-wrapper.py       -t    test-cases/test-cases-metadata/${TEST_NAME}    -o     ${OUTPUT}    -i    ${INPUT}     stdout=stdout_${TEST_CATEGORY}_${TEST_NAME}.txt    stderr=stderr_${TEST_CATEGORY}_${TEST_NAME}.txt
    Should Be Equal As Integers          ${handle.rc}    0

metadata-04-tr.tst
    ${handle} =         Run Process      test-cases/icclim-test-wrapper.py       -t    test-cases/test-cases-metadata/${TEST_NAME}    -o     ${OUTPUT}    -i    ${INPUT}     stdout=stdout_${TEST_CATEGORY}_${TEST_NAME}.txt    stderr=stderr_${TEST_CATEGORY}_${TEST_NAME}.txt
    Should Be Equal As Integers          ${handle.rc}    0

metadata-05-txx.tst
    ${handle} =         Run Process      test-cases/icclim-test-wrapper.py       -t    test-cases/test-cases-metadata/${TEST_NAME}    -o     ${OUTPUT}    -i    ${INPUT}     stdout=stdout_${TEST_CATEGORY}_${TEST_NAME}.txt    stderr=stderr_${TEST_CATEGORY}_${TEST_NAME}.txt
    Should Be Equal As Integers          ${handle.rc}    0

metadata-06-tnx.tst
    ${handle} =         Run Process      test-cases/icclim-test-wrapper.py       -t    test-cases/test-cases-metadata/${TEST_NAME}    -o     ${OUTPUT}    -i    ${INPUT}     stdout=stdout_${TEST_CATEGORY}_${TEST_NAME}.txt    stderr=stderr_${TEST_CATEGORY}_${TEST_NAME}.txt
    Should Be Equal As Integers          ${handle.rc}    0

metadata-07-tnn.tst
    ${handle} =         Run Process      test-cases/icclim-test-wrapper.py       -t    test-cases/test-cases-metadata/${TEST_NAME}    -o     ${OUTPUT}    -i    ${INPUT}     stdout=stdout_${TEST_CATEGORY}_${TEST_NAME}.txt    stderr=stderr_${TEST_CATEGORY}_${TEST_NAME}.txt
    Should Be Equal As Integers          ${handle.rc}    0

metadata-08-dtr.tst
    ${handle} =         Run Process      test-cases/icclim-test-wrapper.py       -t    test-cases/test-cases-metadata/${TEST_NAME}    -o     ${OUTPUT}    -i    ${INPUT}     stdout=stdout_${TEST_CATEGORY}_${TEST_NAME}.txt    stderr=stderr_${TEST_CATEGORY}_${TEST_NAME}.txt
    Should Be Equal As Integers          ${handle.rc}    0

metadata-09-txn.tst
    ${handle} =         Run Process      test-cases/icclim-test-wrapper.py       -t    test-cases/test-cases-metadata/${TEST_NAME}    -o     ${OUTPUT}    -i    ${INPUT}     stdout=stdout_${TEST_CATEGORY}_${TEST_NAME}.txt    stderr=stderr_${TEST_CATEGORY}_${TEST_NAME}.txt
    Should Be Equal As Integers          ${handle.rc}    1

metadata-10-tr-err.tst
    ${handle} =         Run Process      test-cases/icclim-test-wrapper.py       -t    test-cases/test-cases-metadata/${TEST_NAME}    -o     ${OUTPUT}    -i    ${INPUT}     stdout=stdout_${TEST_CATEGORY}_${TEST_NAME}.txt    stderr=stderr_${TEST_CATEGORY}_${TEST_NAME}.txt
    Should Be Equal As Integers          ${handle.rc}    1
