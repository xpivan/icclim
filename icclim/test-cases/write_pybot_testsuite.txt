# Simple one-liner to write template for pybot test cases.
find . -type f -iname '*.tst' -ls | grep 'tst$' | sed 's%.*\./test-cases/\(.*\)%\1%' | awk -F / '{print $2;print "    ${handle} =         Run Process      test-cases/icclim-test-wrapper.py       -t    test-cases/"$1"/"$2"    -o     ${OUTPUT}    -i    ${INPUT}     stdout=stdout_${group1}.txt     stderr=stderr_${group1}.txt";print "    Should Be Equal As Integers          ${handle.rc}    0";print ""}'
