# Simple one-liner to write template for pybot test cases.
find . -type f -iname '*.tst' -ls | grep 'tst$'  | sed 's%.*\./test-cases/\(.*\)%\1%' | awk -F / 'BEGIN {folder="empty"} {if ($1 != folder){print "";print "";print "";print $1;folder=$1} else {print "    ${plot_name}        ${group1} =      Should Match Regexp     ${TEST_NAME}    test-cases-(.*)";print "    ${handle} =         Run Process      test-cases/icclim-test-wrapper.py       -t    test-cases/${TEST_NAME}/"$2"    -o     ${OUTPUT}    -i    ${INPUT}     stdout=stdout_${group1}.txt     stderr=stderr_${group1}.txt";print "    Should Be Equal As Integers          ${handle.rc}    0"};print ""}'
